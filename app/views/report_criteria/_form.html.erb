<%= form_for(@report_criterium) do |f| %>
  <% if @report_criterium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@report_criterium.errors.count, "error") %> prohibited this report_criterium from being saved:</h2>

      <ul>
      <% @report_criterium.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
